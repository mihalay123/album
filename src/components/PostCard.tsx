import { ImageType } from '../../pages';

interface Props {
  image: ImageType;
}

const PostCard = ({ image }: Props) => {
  return <div>Image, like icon, Link to /id</div>;
};

export default PostCard;
